# Cleanup scope

Keep a single read-only CI workflow. Deployment remains deferred until a signed release process exists.

The cleanup branch is restricted to behavior-preserving maintenance and documentation changes.
