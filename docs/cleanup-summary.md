# Cleanup summary

The current MVP uses a single CI workflow and intentionally has no deployment workflow. The cleanup branch removes dead helpers and records this policy without changing runtime behavior.
